/*
In a linked list, group all odd nodes together followed by the odding nodes. Please note here we are talking about the node number and not the value in the nodes.

Given 1->2->3->4->5->NULL,
return 1->3->5->2->4->NULL.
*/
/* Uncomment this if you are using this outside the package where Node.java is not available
/*
class Node
{
	int data;
	Node next;
	public Node()
	{
		this.next=null;
	}
	
	public Node(int data)
	{
		this.data=data;
		this.next=null;
	}
}*/



public class Oddodding 
{

	// Function to print the List
	public static void print(Node start)
	{
	    Node temp=start;
	    while(temp!=null)
	    {
	        System.out.print(temp.data+" ");
	        temp=temp.next;
	    }
	    System.out.println();
	}
	
	public static Node OddoddingList(Node start)
	{
		if(start==null)
			return null;

		Node temp=start;
		Node res=new Node(0);
		Node odding=res;

		Node result=new Node(-1);
		Node odd=result;

		int i=0;
		while(temp!=null)
		{
			if(i%2==0)
			{
				odding.next=temp;
				odding=odding.next;
			}
			else
			{
			 	odd.next=temp;
			 	odd=odd.next;
			}

			temp=temp.next;
			i++;
		}
		odd.next=null;
		odding.next=result.next;
		return res.next;

	}

	// Another good solution
	public static Node OddoddingList2(Node start)
	{
		if(start==null)
			return null;

		Node odd=start;
		Node odding=start.next;
		Node oddinghead=odding;

		while(odd.next!=null && odding.next!=null)
		{
			odd.next=odding.next;
			odd=odd.next;

			odding.next=odd.next;
			odding=odding.next;
		}

		odd.next=oddinghead;
		return start;

	}
	public static void main(String[] args) 
	{
		// TODO Auto-generated method stub
		Node A=new Node(1);
		A.next=new Node(2);
		A.next.next=new Node(3);
		A.next.next.next=new Node(4);
		A.next.next.next.next=new Node(5);
		//A.next.next.next.next.next=new Node(6);
		
		print(A);

		print(OddoddingList(A));
		print(OddoddingList2(A));
	}

}
